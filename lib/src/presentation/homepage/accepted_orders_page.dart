import 'package:flutter/material.dart';

class AcceptedOrdersPage extends StatelessWidget {
  const AcceptedOrdersPage();
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('LOALOA'),),);
  }
}
